{% extends "base.html.twig" %}

{% block body %}
<div class="row mt-5">
    <div class="col-2"></div>
    <div class="col-8 ">
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            {{form(form_comment)}}
        {% endif %}
{% endblock body %}