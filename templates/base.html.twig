<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link href="https://fonts.googleapis.com/css?family=Baloo&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css?family=Montserrat&display=swap" rel="stylesheet">
        

        {% block stylesheets %}

            {{ encore_entry_link_tags('app') }}

        {% endblock %}
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css" integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf" crossorigin="anonymous">


    </head>
    <body>

        <header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light justify-content-start border " style="position: fixed; bottom: 0; right: 0; left: 0; z-index: 99;  -top: 1px solid black">
      

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar_home navbar-nav mx-auto align-items-center  justify-content-around p-1 ">
      <a href="{{path('homepage')}}"><img class="w-50  " src="{{ asset('build/images/logo.png' ) }}" alt="LOGO"></a>
      <button class="navbar-toggler " type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <li class="nav-item ">
        <a class="nav-link"  href="{{ path('pageCategory', {'id' : 1}) }}">Vêtement <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link"  href="{{ path('pageCategory', {'id' : 2}) }}">Accessoires</a>
      </li>
      <li class="nav-item">
        <a class="nav-link"  href="{{ path('pageCategory', {'id' : 4}) }}">Maison</a>
      </li>
      <li class="nav-item">
        <a class="nav-link"  href="{{ path('pageCategory', {'id' : 5}) }}">Fournitures</a>
      </li>
      <li class="nav-item">
        <a class="nav-link"  href="{{ path('pageCategory', {'id' : 6}) }}">Peluche</a>
      </li>

      <li>
        <form method"POST" action="{{ path('homepage') }}" class=" form-inline d-flex justify-content-center md-form form-sm mt-0">
          <input name="search" class="form-control form-control-sm ml-3 w-75" type="search" placeholder="Search"
            aria-label="Search">
            <i class="nav-link fas fa-search" aria-hidden="true"></i>

        </form>
      </li>

        <li class="nav-item ">
            <a class="nav-link"  href="{{ path('contact_form') }}">Contact <span class="sr-only">(current)</span></a>
          </li>
      {% if not app.user %}
      <li class="nav-item ">
        <a class="nav-link"  href="{{ path('security_login') }}"> Connexion <span class="sr-only">(current)</span></a>
      </li>

      {% else %}
        <li class="nav-item ">
        <a class="nav-link"  href="{{ path('profil_user') }}"> Profil <span class="sr-only">(current)</span></a>
      </li>
        <li class="nav-item ">
        <a class="nav-link"  href="{{ path('security_logout') }}"> Deconnexion <span class="sr-only">(current)</span></a>
      </li>
      {% endif %}
      <li class="nav-item d-flex">
        <i class="nav-link fas fa-shopping-cart" href="{{ path('cart_index') }}" aria-hidden="true"></i>
         <a class="nav-link"  href="{{ path('cart_index') }}">Panier</a>
        <p id="js_pillCart"class="badge badge-danger p-1 mr-2">{{app.session.get('panier')|length}}</p>
      </li> 
      <li>
          <a class="nav-link"  href="{{ path('legal_disclaimers') }}">Mentions légales</a>
      </li>
         
    </ul>
  </div>
</nav>
      </header>


        {% block body %}{% endblock %}

  <!-- Position it -->
<div id="toastModel" class="toast" role="alert" aria-live="assertive" aria-atomic="true" data-delay="5000">
  <div class="toast-header">
    <strong class="mr-auto">Produit ajouté au panier</strong>
    <button type="button" class="ml-2 mb-1 close" data-dismiss="toast" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>
  <div class="toast-body">
    Hello, world! This is a toast message.
  </div>
  <div class="text-right"> <a class="btn btn-sm btn-outline-secondary" href="{{ path('cart_index')}}">Voir le panier </a> </div>
</div>

  <div id="js_containerToast">
  </div>

 
    
  

        {% block javascripts %}
        <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>

            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </body>
</html>
