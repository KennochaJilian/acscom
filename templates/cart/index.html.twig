{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}

    <div class="bg-light p-3">
        <h1>Votre panier</h1>
    </div>

    {% if items | length > 0 %}
    
        {% include 'cart/_cartDisplay.html.twig' with {items:items}  %}

        <a href="{{ path('order')}}" class="btn btn-success"> Next step !</a>

    {% else %}
        <p>Votre panier est vide</p>
        
    {% endif %}

{% endblock %}
